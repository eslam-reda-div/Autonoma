/** @type {import("eslint").Linter.Config} */
const config = {
  // Empty configuration to disable all linting
  rules: {
    // This disables all rules
  },
  // Ignore all files
  ignorePatterns: ["**/*"]
};

module.exports = config;
